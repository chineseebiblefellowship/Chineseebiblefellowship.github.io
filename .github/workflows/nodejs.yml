{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "Your Node.js project",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    // 项目的依赖包
  }
}
